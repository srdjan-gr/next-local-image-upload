/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // loader: "akamai",
    // path: "/",
    // domains: ["localhost:3001"],
  },
  // {
  //   loader: "akamai",
  //   path: "localhost:3001",
  // },
};

module.exports = nextConfig;
